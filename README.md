
# AI TRADING PRO V6

## Setup
1. Copy `.env.example` to `.env` and fill keys
2. npm install
3. npm run dev (frontend)
4. npm start (backend)

## Notes
- AUTO_TRADE=false by default (safety)
- Adjust qty in server.js before live trading
